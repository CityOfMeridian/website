<% if flash.any? %>
  <div class="flashes">
    <% user_facing_flashes.each do |key, value| -%>
      <div class="alert alert-<%= key %> alert-dismissable"><%= value %></div>
    <% end -%>
  </div>
<% end %>
